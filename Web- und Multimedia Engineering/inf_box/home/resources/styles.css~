body{
	font-family: 'Ubuntu', sans-serif;
   font-size:14px;
   line-height: 145%;
   font-style: normal;
   padding: 0px;
   margin: 0px;
}
h1 {
    color:black;
    font-family: 'Ubuntu Condensed', sans-serif;
    font-size: 3em;
    line-height: 140%;
    font-style: normal;
}
p{
    color:#666666;
}
/*El objeto header es todo el contenedor de la barra de hasta arriba. Solo defino el fondo*/
header{
	background: -webkit-linear-gradient(#d0d0d0, #efefef); /* For Safari 5.1 to 6.0 */
	background: -o-linear-gradient(#d0d0d0, #efefef); /* For Opera 11.1 to 12.0 */
	background: -moz-linear-gradient(#d0d0d0, #efefef); /* For Firefox 3.6 to 15 */
	background: linear-gradient(#d0d0d0, #efefef); /* Standard syntax (must be last) */
	border-color:transparent;
	border-style:solid; 
	border-top-color:#7a7a7a; 
	border-bottom-color:#cccccc; 
	height: 80px;
	border-width:1px;
	/*Creo que no debe llevar borde arriba...*/
	border-top-width: 0px;
	
}
/*Main es el contendor principal*/
main {
	width:1170px;
	padding:5px; 
	margin-right: auto;
   margin-left: auto;
}
/* contenedor dentro del header, del mismo ancho que el main para colocar botones y logo*/
nav.head{
	width:1170px;
	height:78px;
   margin-right: auto;
   	margin-left: auto;
}
/*los elementos que van en el header tienen todo esto en comun*/
*[class*="header"]{
	height:74px;
	color:#666666;
   text-decoration: none;
	padding-left: 20px;
	padding-right: 20px;
	border-color:transparent;
	border-right-width:1px;
	border-left-width: 0px;
	border-style:solid; 
	border-right-color:#cccccc;
	margin-left: -4px;
}

/* SPRITES */
/*el logo tiene id=logo. Para que solo muestre la mitad de la imagen y tenga un poco de espacio a la derecha*/
.logo {
    height: 80px;
    overflow: hidden;
    display:inline-block;
    padding-right: 60px;
}
/*para que cambie la imagen cuando el puntero esta encima*/
.logo img:hover{
	margin-top: -80px;
}

/*Los hipervinculos de clase header*/
a[class*="header"]{
	text-align:center;
	line-height: 80px;/* TODO: revisar si podemos centrar esto de otra manera */
	display: inline-block;
	/*La transicion de colores*/
	-webkit-transition: background 2s, color 1s; /* For Safari 3.1 to 6.0 */
   transition: background 2s, color 1s;
}
/*Estado final de la transicion de colores, cuando el mouse esta sobre el elemento (hover)*/
a[class*="header-trans"]:hover{
	background: #cccccc;
	color:#1a1a1a;
}
a[class*="selected"]{
	background: #666666;
	color:#f0f0f0;
}

/* ===== BODY===== */

nav.body{
	width:100%;
	height:auto;
	color:#999999;
	padding: 5px;
   	padding-bottom: 30px;
}
nav.body div{
	margin-right: 25px;
}
nav.body a{
	color: #2ed110;
}
nav.body a:hover{
	color: #108500;
	text-decoration: none;
}
nav.body a.active{
	color: #cccccc;
}
nav.body a.active:hover{
	text-decoration: none;
}
/*la tabla de contenido principal tiene esta clase*/
#filelist {
	width:100%;
	color:black;
   margin-right: auto;
   margin-left: auto;
   border-collapse: collapse;
   
}
/*lo use para el header de la tabla principal*/
#filelist th {
	font-weight: bold;
	text-align: left;
	border-bottom:3px solid #cccccc; 
}
#filelist td {
	padding: 5px;
	vertical-align: top;
	padding-bottom: 10px;
	border-bottom: 1px solid #eeeeee;
}
#filelist td.actions{
	color:#999999;
}
#filelist a.icon{
	color:#2ed110;
}
#filelist a.icon:hover{
	color:#108500;
}
#filelist img {
	margin-right: 10px;
}
footer{	
	margin-right: auto;
   margin-left: auto;
   width:1170px;
   	padding:5px; 
	margin-top: 5px;
   border-color:white;
	border-top-width:1px;
	border-style:solid; 
	border-top-color:#2ed110; 
}
/*el el footer meti dos divs. Tienen esto en comun*/
div[class*="footer"]{
	 color:#666666;
    font-size:12px;
    display:inline;
}

/*Defino el tipo del boton de los popups*/
*[class*="grn-btn"] {
	color: white;
	background-color: #00c700;
   font-weight:bold;
   line-height:normal;
	font-size:100%;
	line-height: 200%;
	border-style:solid;
	border-width: 1px;
	-webkit-border-radius: 2px;
	-moz-border-radius: 2px;
	border-radius: 5px;
	border-color:#dddddd;
	vertical-align: middle;
}
/*Es para el checkbox de remember me pero no supe como centrarlo con respecto al boton ????????*/
*[class*="valign"]{
	line-height: 300%;
}

/*HOVER POPUP LINKS (me base en una pagina... asi se ve un CSS bien planeado)*/

#popup a, #popup a:visited {
	position: relative;
}

#popup a span {
	display: none;
}

#popup a:hover span {
	display: block;
	line-height: 140%;
	position: absolute;
	top: 77px;
	right: -1px;
	width: 240px;
	margin: 0px;
	padding: 28px;
	padding-bottom: 17px;
	color: #a1a1a1;
	font-weight: normal;
	background: #fcfcfc;
	border: 1px solid #cccccc;
	border-top-color: #2ed110;
}
#popup a:hover span table{
	width: 100%;
}
#popup a:hover span input{
	border-color: #dddddd;
	background-color: #fafafa;
	-webkit-border-radius: 2px;
	-moz-border-radius: 2px;
	border-radius: 2px;
	border-style: solid;
	border-width: 1px;
	width:100%;
	height:25px;
	line-height: 140%;
}

/* ALIGN */
/*si la clase contiene left, que se vaya a la izq*/
*[class*="left"]{
	float: left;
}
*[class*="right"]{
	float:right;
}
/*si la clase contiene txtleft, que el texto se alinee a la izq*/
*[class*="txtleft"]{
	text-align: left;
}
*[class*="txtright"]{
	text-align: right;
}

*[class*="info"]{
	color: black;
}
/* Horizontal ruler popup */
hr {
    display: block;
    height: 1px;
    border: 0;
    border-top: 1px dashed #cccccc;
    margin: 1em 0;
    padding: 0;
}


/* TEMP */
/*
table, th, td {
   border: 1px solid black;
}
/**/

